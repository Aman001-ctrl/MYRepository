const defaultOptions = {
    maxLength: 200,
    // locale: "en-IN"
}
const localeMap = {
    "$":"en-US",
    "Ã¢â€šÂ¬":"de-DE",
    "Ã‚Â£":"en-GB",
    "Ã‚Â¥":"ja-JP",
    "Ã¢â€šÂ¹":"en-IN",
}
const sign = "(?:-|\+)?";
const digitsAndSeparator = "(?:\d+|\d{1,3}(?:,\d{3})+)";
const decimalPart = "(?:\.\d{1,2})?";
const symbol = "(?:\$|Ã¢â€šÂ¬|Ã‚Â¥|Ã¢â€šÂ¹)?";

const currencyCheckRegex = /^\s*(?:-|\+)?(?:\d+|\d{1,3}(?:,\d{3})+)?(?:\.\d{1,2})?\s*(?:\$|Ã¢â€šÂ¬|Ã‚Â¥|Ã¢â€šÂ¹)?\s*$/u;

export default class CurrencyParser{
    constructor(options){
        this.options = options || defaultOptions;
    }
    parse(val){
        if (typeof val === 'string' && val.length <= this.options.maxLength) {
            if(val.indexOf(",,") !== -1 && val.indexOf(".." !== -1)){
                const match = val.match(currencyCheckRegex);
                if(match){
                    const locale = this.options.locale || localeMap[match[2]||match[5]||"Ã¢â€šÂ¹"];
                    const formatter = new Intl.NumberFormat(locale)
                    val = val.replace(/[^0-9,.]/g, '').trim();
                    val = Number(val.replace(formatter.format(1000)[1], ''));
                }
            }
        }
        return val;
    }
}
CurrencyParser.defaultOptions = defaultOptions;
